jsprimerを読んで学んだこと

後方互換性＝過去に書いたコードが新しい仕様や環境でも動作し続けること
複数コメントも書ける
エクマスクリプトはフロントでもバックでもどの実行環境でも使える機能
ｊｓはそれ以外（読み返すときはサイトの図を見るとわかり安い）
strict modeを宣言すると古いコードの書き方を禁止してくれるからおすすめ
このサイトは使わないと宣言した場合を除き暗示的にこれが使われる
エラーは構文エラーと実行時エラーがある
syntaxは構文エラー
index.js:1:13は一行目１３列目ってこと
実行時エラーはrefarenceError
リテラルは値そのものをコードに直接書き込むための決まった書き方
リテラル＝特定の文字列や数字を検索する機能
演算子のところのビットの後半部分があんまりかも
｜｜は＆＆の逆（読み返したときにない書いてるかわからんと思うけどサイトをしっかり読めばわかる）
nullish(??) = null & undefind
3項条件式めっちゃ便利

ここから黙示的な型変換以降の章
NUNを避けるべし
関数で引数を複数作って呼び出すときに引数が足りないときでもundefindが引数に入れられて実行される
任意の個数の引数を受け取れることを可変長引数という
ファンクションをリターンのところに書くと再帰的な挙動を作れる
this？の問題が多く解決できるのと意図した挙動を読み取りやすいことから基本アロー関数で書き、あかんときはファンクションを使え
オブジェクトのプロパティとはキーとバリューがセットになったもの
関数のなかでメソッドを定義することで関数を関数の中にも作れる
コールバック関数理解浅い、関数の引数に関数をとったもの？
repl=read,eval,print,loop二個目は評価って意味
falsyじゃないものは基本的にtrueとして処理されるから、条件式にfalsyじゃないもの（数字とかでも）を入れたらtrueとして処理される
条件に一致するものをIF文の中に書いて、一致しないものをelseの中に書くいめーじ
式の評価結果が数個程度の時はスイッチ文のほうがいいかも
スイッチめっちゃ書きやすくない？？
でもスイッチ文はあんまよくないらしい
forEachメソッドで配列のすべての要素にアクセスするいわばループ文みたいなことができる
ループ処理を抜け出すのにはbreakを使うとよい
continueもループを途中でやめて、次の数から新しくループさせる
配列を反復処理するメソッドのところ理解浅い
配列のfilterメソッドは不要なものを取り除くという意味でのfilterでもある
文字列を改行するときはバッククオートを使える
クオートを複数使うときはバックスラッシュでエスケープする必要がある
オブジェクトのとこと、配列のとこと関数のとこが大事
文字列のとこも大事
マッチした文字列の取得ってとこから
JSはすべてがオブジェクトであるといわれることがある（ラッパーオブジェクトなどで）
スコープは基本的に内から外なら変数を参照できる
例外処理のファイナリーブロックのところは最後に絶対実行される
開発者モードのスタックトレースのところに
なんの処理でエラーが発生したかが書かれている
同期処理＝上から順番
非同期処理＝上から順番だけど、終わるのを待たない（タスクを登録して、指定の秒数後に実行するイメージ）
npmとnpxにおいて、後者は簡易的に実行できる
domっていうのは素のhtmlをオブジェクトに読み替えたもの。それを操作することによって、表示を書き換えたりできる
http通信とはブラウザとサーバーの通信のこと
で、データのやり取りをする
fetchAPIはjsからhttp通信をするために使う
レスポンスステータスのイメージ
あなた：
「このユーザーの情報ください！」

サーバー：
「OK、あったよ！（200）」
「そんな人いないよ（404）」
「サーバー壊れてる（500）」

これがレスポンスステータス　こんな感じ
webでの実行環境と、ノードjsの実行環境がある
